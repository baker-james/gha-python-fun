name: my-basic-workflow

on: 
  push:
    
env:
  blah: Workflow level env, defined before run-name


run-name: Pipeline run by ${{ env.blah }}


jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-python@v5
        with: 
          python-version: '3.13'
      - run: echo ${{  github.ref  }}
      - run: echo ${{  github.ref == 'refs/heads/master' }}
      - run: python main.py